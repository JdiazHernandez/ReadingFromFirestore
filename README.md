# ReadingFromFirestore
Small JS script to read a firestore DB by name

The key is the introduction of search from key components on the document and not from id.
This code allow to searh cities by name.
